# Portul aplicatiei
server.port=8080

# Configurare MySQL (URL-ul este critic!)
# Asigura-te ca baza de date "demo_db" exista (sau schimba numele cu una existenta)
spring.datasource.url=jdbc:mysql://localhost:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
# Daca ai pus parola la instalare, scrie-o aici. Daca nu, las-o goala sau sterge linia.
spring.datasource.password=Asusvici10

# Configurare Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect